## @package _global_ # remove first # to enable merging into top-level namespace
model: degradation
model_architecture: DegradeNet
netG:
  net_type: DegradeNet
  description: "DegradeNet"
  in_channels: 1
  out_channels: 1
  num_feats: 64
  use_checkpoint: true

  init_type: default
  init_bn_type: uniform  # "uniform" | "constant"
  init_gain: 0.2