defaults:
  - dataset: cifar10

hydra:
  searchpath:
    - pkg://additional_conf
    # You can also use file based schema:
    # - file:///etc/my_app
    # - file://${oc.env:HOME}/.my_app

config: exp_EDDSR